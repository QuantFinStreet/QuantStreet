# Server
PORT=4000
NODE_ENV=development

# CORS — the client's Vite dev origin
CLIENT_ORIGIN=http://localhost:5173

# Milestone 2 (AI Chat) will add an LLM provider key here, e.g.:
# ANTHROPIC_API_KEY=
